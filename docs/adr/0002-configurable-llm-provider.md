# 可配置 LLM Provider 架构

预计 30-50% 的客户有数据不出内网的安全合规要求，无法调用外部大模型 API。我们在架构上抽象统一的 LLM Provider 接口，支持云端 API（Claude、GPT-4o）和本地模型（Ollama、vLLM）按项目配置切换，所有本地模型通过 OpenAI 兼容接口接入以保证零切换成本。

## Consequences

本地模型在复杂推理和视觉理解能力上弱于云端模型，内网场景下部分功能（如视觉兜底定位）的准确率可能下降。这是已知权衡，不是缺陷。
